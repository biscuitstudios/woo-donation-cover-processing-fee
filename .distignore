# Files excluded from the deploy artifact (WP-CLI `wp dist-archive` format).
# Everything here is dev-only or internal and must never reach a live server.
/.distignore
/.gitignore
/.git
.DS_Store
/docs
/CLAUDE.md
/tests
/composer.json
/composer.lock
/phpunit.xml.dist
/node_modules
/bin
/.github
/README.md
/SECURITY.md
/.claude
/.phpunit.result.cache
